<h1>Ajouter un nouveau signet</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.button :submit, "Créer un signet", class: "btn btn-primary" %>
<% end %>
